spring:
  application:
    name: reminder

  profiles:
    active: dev

  jackson:
    time-zone: UTC

app:
  cors:
    # 쉼표 구분 패턴 — 인증/쿠키 도입 시 환경별로 override.
    allowed-origin-patterns: http://localhost:3000

  datasource:
    url: jdbc:h2:mem:reminderdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

---
spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    com.bong.reminder: DEBUG
